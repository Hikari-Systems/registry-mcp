services:
  registry-mcp:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ../../configs/registry-mcp:/sandbox
    healthcheck:
      test: ["CMD", "/app/registry-mcp", "healthcheck"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3
